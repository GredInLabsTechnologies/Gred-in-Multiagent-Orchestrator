<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network for GIMO Core communication -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- Foreground service for mesh agent -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- BLE Wake (API 31+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- BLE Wake (API 28-30 compat) -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- Boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".GimoMeshApp"
        android:label="GIMO Mesh"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.GIMOMesh"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:extractNativeLibs="true"
        android:hasFragileUserData="true"><!-- Fase D2: uninstall-time "Keep app data" checkbox
             so the user decides whether downloaded models (~2-3 GB) survive
             uninstall. When the user does NOT keep data, Android wipes the
             app's externalMediaDirs along with filesDir, leaving no ~3 GB
             orphans. Pattern used by WhatsApp, Mihon, etc. since Android 10.
             Only triggers when uninstalling from Settings (not Play Store). -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link: gimo://enroll?code=XXXXXX&host=IP&port=9325 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="gimo" android:host="enroll" />
            </intent-filter>
        </activity>

        <!-- Mesh Agent Foreground Service — exported temporalmente para
             Fase B adb validation (revert antes de ship). -->
        <service
            android:name=".service.MeshAgentService"
            android:foregroundServiceType="connectedDevice"
            android:exported="true" />

        <!-- BLE Wake Receiver -->
        <receiver
            android:name=".ble.BleWakeReceiver"
            android:exported="false" />

        <!-- Boot Receiver — re-registers BLE scan after reboot -->
        <receiver
            android:name=".ble.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
