# Production Dependencies
# Install with: pip install -r requirements.txt

# Licensing & Cryptography (GIMO License System)
cryptography>=43.0.0       # Ed25519 signing/verification, AES-256-GCM cache
PyJWT[crypto]>=2.9.0       # JWT creation and verification (Ed25519 algorithm)
httpx>=0.28.1              # Async HTTP client; pin aligned with fastmcp>=3.1 (plan CROSS_COMPILE 2026-04-16)

# Core Web Framework
fastapi>=0.128.0           # Align with starlette>=0.49 CVE fix (plan CROSS_COMPILE 2026-04-16)
uvicorn[standard]>=0.34.2
starlette>=0.49.1  # Security fix CVE-2025-62727

# Data Validation
pydantic>=2.11.7            # Align with fastmcp>=3.1 (plan CROSS_COMPILE 2026-04-16)

# HTTP Client
requests==2.33.1
python-dotenv==1.2.1
rich==14.3.2
typer>=0.12.5
PyYAML>=6.0.2

# CLI TUI Dashboard
textual>=0.83.0

# Observability (OpenTelemetry)
# Protocolo HTTP en vez de gRPC: evita la dependencia en `grpcio` (~20 min
# compile desde sdist en Android/Termux y no hay wheels aarch64-linux-android
# pre-built). El wire protocol HTTP/protobuf es semánticamente equivalente
# al de gRPC para OTel — mismo data, distinto transporte. El endpoint sigue
# configurándose vía OTEL_EXPORTER_OTLP_ENDPOINT env var pero debe apuntar
# al puerto HTTP (4318) en vez del gRPC (4317).
opentelemetry-api==1.27.0
opentelemetry-sdk==1.41.0
opentelemetry-exporter-otlp-proto-http==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0

# Multipart Form Data (for file uploads)
python-multipart>=0.0.22  # Security fix CVE-2026-24486

# HTTP/Network (updated for security)
urllib3>=2.6.3  # Security fixes CVE-2025-66418, CVE-2025-66471, CVE-2026-21441

# File Lock Utilities
filelock>=3.20.3  # Security fixes CVE-2025-68146, CVE-2026-22701

# Test Performance
pytest-timeout>=2.3.1     # Global per-test timeout to catch hangs
pytest-xdist>=3.5.0       # Parallel test execution with -n auto

# Hardware Monitoring
psutil>=6.0.0  # CPU/RAM monitoring for intelligent model routing
nvidia-ml-py>=12.0.0 # NVIDIA GPU memory monitoring (replaces deprecated pynvml)

# ASN.1 Support (dependency of cryptography)
pyasn1>=0.6.2  # Security fix CVE-2026-23490

# Mesh LAN Discovery (opt-in via ORCH_MDNS_ENABLED=true)
zeroconf>=0.132.0           # mDNS service advertisement for Core discovery

# Runtime Packaging (signed wheelhouse forge)
# Base: github.com/GredInLabsTechnologies/rove @ v1.0.0 + patch local 1.0.1
# añadiendo target `android-x86_64` — requerido para APK agnóstico (Chromebook
# ARC, tablets Android x86, emulator testing). Patch source: /tmp/rove-src
# (branch local, no upstream todavía).
./vendor/rove/rove_toolkit-1.0.1-py3-none-any.whl
zstandard>=0.22             # zstd bundle compression (used by rove)

# MCP Support (for App façade and bridge)
fastmcp>=3.1.0              # MCP server implementation
mcp>=1.26.0                 # MCP protocol types
sse-starlette>=3.3.0        # Server-Sent Events for MCP streaming

# Note: Removed 128+ unused dependencies including:
# - ML/AI: torch, transformers, opencv, onnxruntime-gpu (not used)
# - PDF: pypdf, xhtml2pdf, reportlab (not used, have vulnerabilities)
# - Google AI: google-generativeai (not used)
# - Many others identified during dependency audit
#
# If any of these are needed in the future, add them back with:
# 1. Clear justification of usage
# 2. Security scan verification
# 3. Documentation in code where used
